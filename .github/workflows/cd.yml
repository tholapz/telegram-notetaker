name: CD

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    name: Build & Push Multi-Platform Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      image-digest: ${{ steps.build.outputs.digest }}
      image-tags: ${{ steps.meta.outputs.tags }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU (for arm64 cross-compile)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-,format=short,event=branch
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=semver,pattern={{version}},event=tag
            type=semver,pattern={{major}}.{{minor}},event=tag
            type=semver,pattern={{major}},event=tag

      - name: Build and push multi-platform image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ============================================================================
  # OPTIONAL: Deploy to Google Cloud Run
  #
  # Prerequisites (one-time setup):
  # 1. Enable GCP APIs:
  #      gcloud services enable run.googleapis.com artifactregistry.googleapis.com
  # 2. Configure Workload Identity Federation (recommended — no long-lived keys):
  #    https://github.com/google-github-actions/auth#setting-up-workload-identity-federation
  # 3. Add GitHub secrets to this repo:
  #      GCP_WORKLOAD_IDENTITY_PROVIDER  — WIF provider resource name
  #      GCP_SERVICE_ACCOUNT             — service account email
  #    OR if using a key file:
  #      GCP_SA_KEY                      — service account JSON key
  # 4. Grant the service account:
  #      roles/run.admin
  #      roles/iam.serviceAccountUser
  # 5. Fill in the three env vars below and uncomment this job.
  #
  # PORT note: Cloud Run injects PORT=8080 automatically. The app reads it via
  # pydantic-settings and the Dockerfile CMD expands ${PORT} — no changes needed.
  # ============================================================================
  # deploy-cloud-run:
  #   name: Deploy to Cloud Run
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   environment: production
  #
  #   env:
  #     GCP_PROJECT_ID: your-project-id        # ← fill in
  #     GCP_REGION: us-central1                # ← fill in
  #     CLOUD_RUN_SERVICE: my-service          # ← fill in
  #
  #   steps:
  #     - name: Authenticate to Google Cloud
  #       uses: google-github-actions/auth@v2
  #       with:
  #         workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
  #         service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
  #         # Alternative (key file): credentials_json: ${{ secrets.GCP_SA_KEY }}
  #
  #     - name: Deploy to Cloud Run
  #       uses: google-github-actions/deploy-cloudrun@v2
  #       with:
  #         service: ${{ env.CLOUD_RUN_SERVICE }}
  #         region: ${{ env.GCP_REGION }}
  #         image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ needs.build-and-push.outputs.image-digest }}
  #         flags: |
  #           --min-instances=0
  #           --max-instances=10
  #           --memory=512Mi
  #           --cpu=1
  #           --concurrency=80
  #           --timeout=30s
  #           --allow-unauthenticated

  # ============================================================================
  # OPTIONAL: Deploy to VPS via SSH
  #
  # Prerequisites:
  # 1. Add GitHub secrets to this repo:
  #      VPS_HOST        — server IP or hostname
  #      VPS_USER        — SSH username (e.g. deploy)
  #      VPS_SSH_KEY     — private key contents (cat ~/.ssh/id_ed25519)
  #      VPS_DEPLOY_PATH — absolute path on server (e.g. /opt/my-service)
  # 2. Copy docker-compose.yml to VPS_DEPLOY_PATH on the server.
  # 3. Create a .env file at VPS_DEPLOY_PATH with production values.
  # 4. Ensure the VPS user is in the docker group.
  # 5. Uncomment this job.
  # ============================================================================
  # deploy-vps:
  #   name: Deploy to VPS
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   environment: production
  #
  #   steps:
  #     - name: Deploy via SSH
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.VPS_HOST }}
  #         username: ${{ secrets.VPS_USER }}
  #         key: ${{ secrets.VPS_SSH_KEY }}
  #         script: |
  #           cd ${{ secrets.VPS_DEPLOY_PATH }}
  #           docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ needs.build-and-push.outputs.image-digest }}
  #           docker tag \
  #             ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ needs.build-and-push.outputs.image-digest }} \
  #             ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
  #           docker compose up -d --remove-orphans
  #           docker image prune -f
